version: "4"

services:
  nginx_container:
    container_name: nginx
    build:
      dockerfile: Dockerfile_nginx
    ports:
      - "80:80"


  apache_container:
    container_name: apache
    build:
      dockerfile: ../task15/Dockerfile_apache
    ports:
      - "8080:80"

